// Copy to defaults.js and fill in your values to prefill the popup fields.
// defaults.js is gitignored -- never commit real URLs or tokens.
const MM_DEFAULTS = {
    serverUrl: "",  // e.g. "https://your.domain/your-webhook-path"
    token: ""       // one of the API_TOKENS values from config.py
};
